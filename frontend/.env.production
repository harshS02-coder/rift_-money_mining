# Production Environment Variables
# This file is used when building for production deployment

# API URL - set to the actual backend deployment URL
# For Railway deployments, use the actual domain
# VITE_API_URL=https://your-backend-domain.railway.app/api

# If backend is on same domain (proxied), use relative path
VITE_API_URL=/api

# Vercel deployment configuration
VITE_VERCEL_URL=
