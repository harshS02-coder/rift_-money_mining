# Frontend Environment Variables

# Development: Uses Vite proxy, no need to set
# VITE_API_URL=/api

# Production: Set this to your Railway backend URL
# VITE_API_URL=https://your-backend-domain.railway.app/api
# Or if frontend and backend are on same Railway service:
# VITE_API_URL=/api

# Leave blank to use relative /api path (works if backend is proxied or on same domain)
VITE_API_URL=
